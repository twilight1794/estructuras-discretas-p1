package xyz.campanita.estructurasdiscretasp1;
/*
import android.app.Dialog;
import android.app.DialogFragment;
import android.os.Bundle;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.appcompat.app.AlertDialog;

public class FiltroTemasDialogFragment extends DialogFragment {
    @NonNull
    @Override
    public Dialog onCreateDialog(@Nullable Bundle savedInstanceState) {
        return new AlertDialog.Builder(this)
                .setTitle(R.string.error_descarga_t)
                .setMessage(R.string.error_descarga)
                .setPositiveButton(android.R.string.ok, (dialog, which) -> {} )
                .setCancelable(false)
                .create();
    }
    public static String TAG = "FiltroTemasDialog";
}
*/